import { DropdownDirectivesDirective } from './dropdown-directives.directive';

describe('DropdownDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
