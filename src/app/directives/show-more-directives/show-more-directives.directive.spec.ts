import { ShowMoreDirectivesDirective } from './show-more-directives.directive';

describe('ShowMoreDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMoreDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
